import React from "react";
import { Route, Routes } from "react-router-dom";

const VideoCallRoutes: React.FC = () => {
    return (
        <Routes>

        </Routes>
    );
};

export default VideoCallRoutes;